#include "main.h"
/**
 * main Entry point
 * prints lowercase alphabets
 * Return: Always 0.
 */
int main(void)
{
	char q = 'a';
	while (q <= 'z')
	
	{
		_putchar(q);
		q++;
	}
	_putchar('\n');
	return(0);
}
