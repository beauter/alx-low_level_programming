#include "main.h"
/**
 * main Entry point
 * prints lowercase alphabets
 * Return: Always 0.
 */
void print_alphabet_x10(void);
{
	char q = 'a';
	while (q <= 'z')
	
	{
		_putchar(q);
		q++;
	}
	_putchar('\n');
	return(0);
}
